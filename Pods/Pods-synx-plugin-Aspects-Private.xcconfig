#include "Pods-synx-plugin-Aspects.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/Aspects" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/Aspects"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}